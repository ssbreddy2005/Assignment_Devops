# Assignment_Devops
# Marginalia
A vivid, block-based note workspace  A Notion-style note-taking app built with plain HTML, CSS, and JavaScript — no build step, no framework, no backend. It runs entirely in the browser, stores notes privately in `localStorage`, and installs as a real app on Android (and iOS/desktop) as a Progressive Web App (PWA). 
